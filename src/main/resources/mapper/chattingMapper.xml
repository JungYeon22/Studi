<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="chattingMapper">

<resultMap id="chatroom_rm" type="chatWebsocket.bean.ChatRoomDTO">
    <id property="chatRoomNo" column="CHATROOMNO"/>
    <result property="title" column="TITLE"/>
    <result property="memberNo" column="MEMBERNO"/>
</resultMap>

<select id="selectChatRoomList" resultMap="chatroom_rm">
    SELECT CHATROOMNO, TITLE, MEMBERNO FROM CHATROOM WHERE STATUS = 'Y' ORDER BY CHATROOMNO DESC;
</select>

    <insert id="openChatRoom" useGeneratedKeys="true" keyProperty="chatRoomNo" parameterType="chatWebsocket.bean.ChatRoomDTO">
        INSERT INTO CHATROOM (TITLE, STATUS, MEMBERNO)
        VALUES (#{title}, 'Y', #{memberNo});
    </insert>

    <insert id="insertMessage">
        INSERT INTO CHATMESSAGE
        VALUES(SEQCMNO.NEXTVAL, #{message}, DEFAULT, #{chatRoomNo}, #{memberNo})
    </insert>

</mapper>
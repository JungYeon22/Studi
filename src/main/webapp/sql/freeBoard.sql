CREATE TABLE `FREE_BOARD` (
`FBOARD`	INT,
`TITLE`	VARCHAR(100),
`DATE`	DATE,
`CONTENT`	VARCHAR(100),
`LIKE`	INT	NULL,
`UserId`	VARCHAR(100)
);


CREATE TABLE `LIKE` (
`FBOARD`	INT,
`UserId`	VARCHAR(100)
);

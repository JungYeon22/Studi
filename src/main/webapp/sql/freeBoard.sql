CREATE TABLE `FREE_BOARD` (
`FBOARD`	INT,
`TITLE`	VARCHAR(100),
`DATE`	DATETIME  default  CURRENT_TIMESTAMP,
`CONTENT`	VARCHAR(100),
`LIKE`	INT	NULL,
`UserId`	VARCHAR(100)
);


CREATE TABLE `LIKE` (
`FBOARD`	INT,
`UserId`	VARCHAR(100)
);
